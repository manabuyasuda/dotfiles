pre-commit:
  parallel: true
  commands:
    textlint:
      glob: "*.md"
      run: npx textlint {staged_files}
    validate-agents-skills:
      glob: "claude/**/*.md"
      run: node scripts/validate-agents-skills.mjs
